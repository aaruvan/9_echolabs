# A5: Three Alternative Uses of Your Public API

## Before running

1. **Production URL** is already set for Team 9 (EchoLabs): `https://vanshiaaru.pythonanywhere.com/api/public/conversations/`

2. Install `requests` if needed: `pip install requests`

## Scripts

- **use_case_1_requests.py** — Calls the public API and prints total users + sample rows.
- **use_case_2_summary.py** — Finds the top user by conversation count.
- **use_case_3_csv_export.py** — Exports API data to `public_api_export.csv`.

## Submission

For each use case:

1. Run the script (from this directory or with the script path).
2. Take a **screenshot** of the output (terminal and/or generated file).
3. Submit the three scripts plus the three screenshots, with a short explanation (1–5 sentences) per use case.
