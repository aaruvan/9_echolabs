# Public API Use Cases

Update the `API_URL` in each script to your production public API URL:
```
https://YOURDOMAIN/api/public/conversations/
```

Run the scripts and capture screenshots of the output.
